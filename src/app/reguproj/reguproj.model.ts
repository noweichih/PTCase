export type ReguProjs = Array<{ 
    regucode: string, 
    projcode: string, 
    proj: string,
   // constructor(regucode: string, projcode: string, proj: string):any
 
  }>;


/*export class ReguProj { 
    regucode: string ;
    projcode: string;
    proj: string
  
    constructor(regucode: string, projcode: string, proj: string) {
      this.regucode = regucode;
      this.projcode = projcode;
      this.proj = proj
    }
  }
  */
